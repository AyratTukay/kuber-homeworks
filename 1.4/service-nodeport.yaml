apiVersion: v1
kind: Service
metadata:
  name: nginx-multi-service
spec:
  selector:
    app: nmulti
  type: NodePort
  ports:
    - protocol: TCP
      name: nginx
      port: 9001
      targetPort: 80
    - protocol: TCP
      name: multitool
      port: 9002
      targetPort: 8080